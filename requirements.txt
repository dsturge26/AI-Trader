# --- AI-Trader dependencies ---
# These use ">=" ("this version or newer") so pip can grab a ready-made
# package for YOUR Python version instead of trying to compile from source
# (which is slow and needs a C compiler you probably don't have).

alpaca-py>=0.33     # Official Alpaca brokerage SDK (data + trading)
pandas>=2.2         # Tables of numbers; we use it to compute indicators
python-dotenv>=1.0  # Reads your secret keys from the .env file
